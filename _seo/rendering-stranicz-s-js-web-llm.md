---
title: Рендеринг страниц с JS (WEB & LLM) в SEO
date: 2026-02-16
layout: default
---

# Как правильно анализировать рендеринг сайтов для SEO специалистов

[>> Вернуться на главную](https://madmanre.github.io/)

#рендеринг #индексация #javascript

## Ситуация с рендерингом на конец 2025 - начало 2026 (WEB)

- Существует 2 неверных мифа:
	- поисковики ничего не рендерят
	- поисковики давно все рендерят
- В данный момент растет количество сайтов "на javascript".
- Даже обычные SEO-friendly движки сайтов (вроде Битрикс) часто на фронтенде используют реактивные фреймворки.
- Существуют SaaS-платформы (например, Tilda), где проблемы с рендерингом могут быть **ограниченно управляемыми или временно нерешаемыми**, так как SEO-специалист не имеет доступа к архитектуре фронтенда. 

Статьи на тему:
- [https://ahrefs.com/blog/javascript-seo/](https://ahrefs.com/blog/javascript-seo/) (очень сильная статья)
- [https://www.clickrank.ai/llms-render-javascript/](https://www.clickrank.ai/llms-render-javascript/)
- [https://www.screamingfrog.co.uk/seo-spider/tutorials/crawl-javascript-seo/](https://www.screamingfrog.co.uk/seo-spider/tutorials/crawl-javascript-seo/)
- [https://vc.ru/seo/1262880-kak-podruzhit-saity-na-javascript-s-seo-ssr-ssg-isg-chto-eto-voobshe-takoe](https://vc.ru/seo/1262880-kak-podruzhit-saity-na-javascript-s-seo-ssr-ssg-isg-chto-eto-voobshe-takoe)
### Официальная позиция Яндекс

Яндекс **может** рендерить JavaScript при включённой настройке рендеринга в Яндекс Вебмастере, однако рендеринг:
- не гарантируется для всех страниц
- может быть ограничен по времени и ресурсам
- нестабилен для сложных сценариев

Также Яндекс говорит **НЕ ВКЛЮЧАТЬ** рендеринг JS при условии, что реализован пререндеринг или серверный рендеринг.

Документация – [https://yandex.ru/support/webmaster/ru/yandex-indexing/rendering](https://yandex.ru/support/webmaster/ru/yandex-indexing/rendering) 
### Официальная позиция Google

Google подробно рассказывает о том, как он взаимодействует с JS.
Ссылка на документацию – [https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics?hl=ru](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics?hl=ru) 

Кратко схему можно описать так: `сканирование страницы > полечение html контента > очередь на отрисовку js контента > индексация`.

Сразу стоит сказать, что для отрисовки страница должна дождаться своей очереди, но как правило в большинстве случаев задержка не критична, **но может быть существенной для новых страниц, больших сайтов и часто обновляемого контента**. 

>Важно: Google явно рекомендует использовать серверный рендеринг или пререндеринг, по причине того, что сайт будет загружаться быстрее и не все роботы могут корректно обрабатывать JS контент.

>Еще нюанс: для Google News явно говорится: *В коде веб-страниц нужно использовать только HTML. Размещать текст статьи внутри элементов JavaScript нельзя.* (https://support.google.com/news/publisher-center/answer/9606708?hl=ru)


![Схема рендеринга страниц с JS указанная в документации Google](/js-website-rendering-scheme.png)
### Что на практике

На практике замечено замечено следующее:

На практике встречались кейсы, когда заявленный рендеринг JS в Яндексе **не приводил к фактическому учёту контента в индексе**, что подтверждалось ответами службы поддержки.

Google в большинстве случае корректно рендерить JS, если нет ошибок в реализации на стороне сайта.
## Как проверить рендеринг

Существуют разные способы проверить рендеринг.
По факту, нам нужно проверить наличие контента в индексе поисковых систем.
В конечном итоге нам неважно каким образом этот контент туда попадает, главное есть он или нет. 
### Проверка в Яндекс

Для проверки в Яндекс можно использовать инструменты Вебмастера или сохраненную копию.

>Важно: при проверки сохраненной копии нужно смотреть текстовую версию, т.к. обычная версия сохраненной копии может показывать некорректные данные или даже 404 ошибку (при отсутствии ошибки как таковой). Дело в том, что сохраненная копия создается не на сайте, а на ресурсе по адресу [https://yandexwebcache.net/](https://yandexwebcache.net/). При отображении страницы часто используются дополнительные файлы со стилями, скриптами и т. д. И если эти ресурсы имеют относительные ссылки (например /script.js), то сервер сохраненных копий пытается их загрузить по несуществующему пути). Более подробно об этом тут – [https://yandex.ru/support/webmaster/ru/site-indexing/saved-copy](https://yandex.ru/support/webmaster/ru/site-indexing/saved-copy)

### Проверка в Google

Для проверки в Google нужно использовать инструмент проверки URL в Google Search Console или аналогичный инструмент [https://search.google.com/test/rich-results](https://search.google.com/test/rich-results). 

В инструменте можно провести эмуляцию процесса обработки страницы поисковым роботом Google и увидеть какой контент видит робот (а также скриншот страницы после рендеринга).

>Важно: не всегда эти инструменты показывают то, что видит реальный робот. Дело в том, что в поиске и в инструменте используются разные user-agent, и если существуют персональные правила обработки на уровне user-agent, то вполне допустимо, что поисковый робот (Googlebot) увидит одно, а инструмент (Google-InspectionTool) другое. Подробнее про user-agent можно почитать тут https://developers.google.com/crawling/docs/crawlers-fetchers/google-common-crawlers?hl=ru 

### Иные типы проверок

Существуют способы проверки рендеринга например в инструменте Screaming Frog Seo Spider: включите рендеринг JS, выберете user-agent Googlebot Mobile и посмотрите страницу после рендеринга. Более подробно можно прочитать в руководстве Screaming Frog – https://www.screamingfrog.co.uk/seo-spider/tutorials/crawl-javascript-seo/

Также существуют сторонние инструменты, например:
- [https://technicalseo.com/tools/fetch-render/](https://technicalseo.com/tools/fetch-render/)
- [https://tamethebots.com/tools/fetch-render](https://tamethebots.com/tools/fetch-render)

Один из самых надежных методов: цитатный поиск. Для этого сделайте так: `site:url "text"`, например: *site:https://vc.ru/seo/1262880-kak-podruzhit-saity-na-javascript-s-seo-ssr-ssg-isg-chto-eto-voobshe-takoe "JavaScript уже после отправки HTML документа"*. Если поиск находит документ и в подсветки вы видите текст, то считается, что поиск содержит этот контент в индексе.

![Цитатный поиск в Google пример](/primer-czitatnogo-poiska-v-google.png)
![Цитатный поиск в Яндекс пример](/primer-czitatnogo-poiska-v-yandex.png)

Если вы смотрите контент, отключая JS в браузере, можно сделать иначе: 
1. Открыть панель веб-разработчика
2. Вкладка network
3. Перезагрузка через F5
4. Выбираем документ из логов
5. Нажмите на вкладку Preview

Но это не лучшая рекомендация, т.к. Google отрисовывает JS, а вы его отключаете.

![Просмотр содержимого страницы без JS с помощью панеле вебмастеров в браузере](/prosmotr-soderzhimogo-cherez-network-preview.png)
### Чек лист проверки

Предлагаемая проверка:
- Цитатный поиск элементов по критически важным для SEO зонам документа в Google / Яндекс.
- Проверка этих же страниц в Google [https://search.google.com/test/rich-results](https://search.google.com/test/rich-results)
- Проверка этих же страницы в сохраненной копии Яндекс
## Распространенные проблемы и решения

Теоретический блок с разбором работы технологий будет ниже. В данном блоке разберем сами ошибки.

❗ Если для доступа к контенту требуется взаимодействие со страницей: нажатие на кнопку, скролл мыши и т.п. То контент с большой долей вероятности может быть недоступен для поискового робота. Чтобы диагностировать проблему нужно понять как именно реализован элемент. Если элемент скрыт в display: none и нажатие на кнопку просто делает контент видимым, то поисковый робот контент сможет получить, потому как display: none является стилизацией, но сам контент в коде доступен. Но если при нажатии на кнопку контент собирает "в моменте силами JS", то такой контент скорее всего останется недоступным. Более подробно читайте далее в блоке "JS события".

❗ Если скрипты, необходимые для отрисовки контента закрыты в robots.txt, то контент останется недоступным для робота, потому что робот не получит доступ к скрипту. Также важно, чтобы весь стек зависимостей (`index.html > main.js > render.js > third_party_api_endpoint`) должен быть доступен, в противном случае риск ошибок очень высокий. 

❗ Если скрипт, который отвечает за рендеринг слишком большой, то есть риск, что поисковая система не считает весь файл (например Google установил максимальный размер обрабатываемых файлов = 2 MB). В таком случае разработчики должны разделить логику скрипта по разным файлам. 

❗ Если скрипт работает очень долго, то робот может не дождаться и не считать информацию. Яндекс дает информацию для разработчиков относительно обработки данной проблемы [https://yandex.ru/support/webmaster/ru/yandex-indexing/rendering#spec](https://yandex.ru/support/webmaster/ru/yandex-indexing/rendering#spec).

❗ Если скрипт требует некоторых разрешений (например доступ к микрофону), то роботы будут отклонять запросы, что с большой долей вероятности может являться причиной отсутствия контента (т.е. робот отклонил разрешение и перестал исполнять скрипт далее).

❗ Если для отображения разного контента используются якоря (реализация через Ajax), то робот может испытывать сложности с индексацией. В документации Google говорится следующее: *Поскольку [поддержка схемы сканирования AJAX была прекращена](https://developers.google.com/search/blog/2015/10/deprecating-our-ajax-crawling-scheme?hl=ru) в 2015 г., фрагменты URL не всегда совместимы с роботом Googlebot.*

❗ Если для загрузки скриптов используются асинхронные вызовы через async, то для поискового робота DOM может строится некорректно. Скрипт загружается в фоновом режиме, не мешая строить страницу. Как только он скачался — выполнение страницы **прерывается**, и скрипт запускается немедленно. Такие ошибки могут быть после работ по "ускорению" страниц, т.к. асинхронная загрузка повышает скорость, но может работать нестабильно. В данном сценарии рекомендуется использовать defer (про async и defer будет рассказано ниже). 

Также рекомендуется ознакомиться с ошибками, представленными в документации Google – [https://developers.google.com/search/docs/crawling-indexing/javascript/fix-search-javascript?hl=ru](https://developers.google.com/search/docs/crawling-indexing/javascript/fix-search-javascript?hl=ru) 

Глобально все проблемы сводятся к 3 возможным сценариям:
- поиск физически не может получить ресурсы
- поиск не дожидается выполнения скриптов
- поиск получает DOM отличный от того, что видит пользователь
## Как делать правильно

Цель корректной реализации страниц с JavaScript для SEO — обеспечить стабильную доступность основного контента для поисковых роботов **без зависимости от выполнения сложного JavaScript и пользовательских действий**.

Важно понимать: поисковые системы не оценивают, _как_ именно реализован рендеринг. Для них имеет значение только результат — **какой HTML и DOM они получают**.

Если очень кратко: 
- все элементы страницы в html – идеально для SEO
- все критически важные элементы в html – обязательно
- остальные элементы (например отзывы, комментарии) – можно подгружать скриптом, но поиск также должен их видеть

### Основной контент должен быть доступен без выполнения JavaScript

Правильной считается реализация, при которой:
- основной текст страницы
- заголовки (H1–H2)
- ссылки на другие страницы

присутствуют в **исходном HTML-документе** или гарантированно доступны при минимальном выполнении JavaScript.

Если при отключённом JavaScript страница:
- полностью пустая
- содержит только заглушки или прелоадеры
    
— такая реализация считается **рискованной для SEO**, особенно для Яндекса.

### Контент не должен зависеть от пользовательских событий

SEO-значимый контент **не должен появляться** только после:
- клика по кнопке
- прокрутки страницы
- наведения курсора
- таймера или отложенного события

Допустимо:
- скрывать контент через стили (например, `display: none`)
- визуально раскрывать его при взаимодействии

Недопустимо:

- собирать или загружать контент «на лету» в момент события

### Все ресурсы, необходимые для рендеринга, должны быть доступны роботам

Для корректного рендеринга необходимо, чтобы:

- JS-файлы
- CSS
- API-запросы
- сторонние сервисы

были доступны для поисковых роботов и **не были закрыты в `robots.txt`**.

Важно учитывать не только основной файл, но и **всю цепочку зависимостей**, от HTML до конечного источника данных.

### Рендеринг должен быть быстрым и стабильным

Корректная реализация предполагает, что:
- страница рендерится без долгих задержек
- выполнение скриптов не блокируется ожиданием разрешений
- отсутствуют тяжёлые и нестабильные сценарии

Поисковые системы имеют ограничения по:
- времени выполнения JavaScript
- объёму обрабатываемых ресурсов

Поэтому чем проще и быстрее формируется контент — тем выше надёжность его индексации.

### Страница должна быть одинаковой для пользователя и робота

Правильная реализация исключает ситуации, когда:
- пользователь видит полноценную страницу
- поисковый робот получает пустую или урезанную версию

Контент, доступный пользователю, должен быть:
- доступен поисковому роботу
- совпадать по смыслу и структуре

Любые расхождения — потенциальный риск для индексации и доверия.

### Результат должен подтверждаться проверками

Корректная реализация всегда подтверждается:
- цитатным поиском в Google и Яндекс
- проверкой через инструменты Google
- проверкой через сохранённую копию Яндекса

Если контент стабильно присутствует во всех проверках — реализация считается корректной для SEO, независимо от используемых технологий.

### Ключевая мысль раздела

> **Для SEO не существует “правильного JavaScript”.  
> Существует только доступный или недоступный контент.**

Если контент:
- виден в индексе
- стабильно воспроизводится
- не зависит от событий и сложного JS

— значит страница реализована правильно с точки зрения SEO.

### Неочевидные приемы оптимизации

Контент, который нужно скрыть (например ссылки на закрытые от индексации страницы фильтров) можно скрыть в data атрибутах. Например если использовать `<a data-herf="https://site.ru/url/filter/param/">` , то для поиска этот элемент не будет ссылкой, значит робот его не использует для переходов, а уже на стороне клиента этот элемент можно преобразовать из data-href в href, тогда пользователи смогут переходить по ссылкам, а роботы нет. 

Если на странице имеется важные контент, который никак нельзя отобразить в html (например график динамики цен акций), то как вариант можно сделать "подложку" для роботов в виде изображения. В примере с акциями можно сделать html таблицу, чтобы передать информацию из графика + картинку самого графика и описание картинки через alt. В момент загрузки, пока не будет отрендерен график можно показывать эту подложку. А таблицу в целом можно оставить в документе.

Существует технология "острова". «React-острова» — это архитектурный подход, позволяющий внедрять интерактивные компоненты React в статические HTML-страницы. Этот метод минимизирует JavaScript, загружая интерактивность только там, где необходимо, обеспечивая высокую производительность и быструю загрузку. Т.е. используя такую технологию, вам будет легко определить критические SEO блоки и переместить их в html.
## Более подробно про страницы с JS

Ниже разберем теоретические аспекты работы технологии для того, чтобы лучше понимать контекст происходящего и получить возможность где-то обосновать свою доработку или распознать моменты, где разработчики явно уходят от сложностей, оперируя концепциями, которые SEO специалисты обычно не понимают.
### Реактивные фреймворки

**Реактивные фреймворки** — это JavaScript-фреймворки, которые строят интерфейс страницы **на основе состояния данных**, а не прямых изменений HTML.  
Разработчик описывает _логику и структуру_, а фреймворк сам решает **когда и какие части страницы перерисовать**.

Проще:

> Меняются данные → фреймворк автоматически обновляет DOM.

Реактивные фреймворки используются для:
- создания сложных интерфейсов (SPA, кабинеты, маркетплейсы)
- динамического обновления контента без перезагрузки страницы
- управления большим количеством состояний (фильтры, корзина, сравнение)
- повышения удобства пользователя

Для бизнеса:
- быстрее разработка
- легче поддержка
- единая логика для фронтенда

Концептуально работают эти фреймворки так:

```
Сервер отдаёт:
- пустой <div id="app">
- подключённый JS-бандл
В HTML нет основного контента или он минимален.
После загрузки страницы:
- браузер скачивает JS
- фреймворк инициализируется
- запускается логика приложения
Для поискового робота это **критический момент**:  
если JS не выполнится — контента не будет.
Фреймворк:
- делает запросы к API
- получает JSON-данные (товар, описание, характеристики)
👉 Пока данные не получены — контент отсутствует.
Фреймворк:
- сопоставляет данные с шаблонами
- формирует виртуальное представление страницы
- сравнивает изменения с предыдущим состоянием
Это внутренняя логика, поисковик её не видит.
Только на этом этапе:
- в DOM появляются H1
- текст
- ссылки
- изображения
Если поисковый робот:
- не дождался этого момента
- столкнулся с ошибкой
- не выполнил JS полностью
👉 контент не будет учтён.
```

На деле реактивные фреймворки заняли рынок, однако помимо явных преимуществ были выявлены и существенные недостатки:
- SEO – даже сейчас существуют сложности с рендерингом и корректной отрисовкой контента на стороне поисковых систем. 
- Скорость загрузки – фреймворки переносят всю нагрузку на клиента, но в этом случае снижается скорость загрузки. Это происходит потому что необходимо пройти целый цикл операций для исполнения скрипта: получить данные, считать их, провести синтаксический анализ, исполнить скрипт. Все это будет в разы медленнее на мобильном устройстве с нестабильным интернетом. Конечно, нагрузка на сервер снижается (улучшается TTFB), но как правильно сильно падает LCP. 
- Безопасность – особенно проблемы с конфиденциальными данными. 

По этим причинам рендеринг на стороне сервера стал, пожалуй, лучшим методом повышения производительности и удобства работы с веб-приложениями. С помощью серверного рендеринга приложения могут быть оптимизированы по скорости и доступности, что приводит к ускорению загрузки, улучшению SEO и повышению вовлеченности пользователей.

На конец 2025 – начало 2026 наиболее распространены:
- **React** (и экосистема: Next.js)
- **Vue.js** (и Nuxt)
- **Angular**
- **Svelte**

Узнать, какой фреймворк использует ваш сайт можно через браузерное расширение wappalazer (определяет не всегда!).

![Браузерное расширение wappalyzer](/brauzernoe-rasshirenie-wappalyzer.png)
#### Основные стратегии рендеринга

##### Client-Side Rendering (CSR)

**В чём смысл**  
Сервер отдаёт минимальный HTML и JavaScript. Весь контент формируется в браузере пользователя (и поискового робота) после выполнения JS.

**Преимущества**
- простая архитектура
- высокая интерактивность
- удобно для сложных SPA
- сниженная нагрузка на сервер

**Недостатки**
- риски для SEO (контент появляется только после JS)
- зависимость от корректного JS-рендеринга у робота
- медленный LCP, особенно на мобильных
- сложная диагностика проблем индексации

##### Server-Side Rendering (SSR)

**В чём смысл**  
Контент формируется на сервере при каждом запросе и сразу отдаётся в HTML. JavaScript подключается для интерактивности.

**Преимущества**
- контент доступен сразу
- высокая стабильность для SEO
- быстрый LCP
- меньше рисков при индексации

**Недостатки**
- выше нагрузка на сервер
- сложнее инфраструктура
- медленнее разработка и поддержка
- возможные задержки при сложных вычислениях

##### Static Site Generation (SSG)

**В чём смысл**  
HTML-страницы генерируются заранее на этапе сборки и отдаются как статические файлы.

**Преимущества**
- максимально быстрый рендеринг
- идеален для SEO
- минимальная нагрузка на сервер
- высокая стабильность

**Недостатки**
- сложно работать с часто меняющимся контентом
- долгие сборки при большом количестве страниц
- не подходит для персонализации

##### Prerendering (Dynamic Rendering)

**В чём смысл**  
Для поисковых роботов отдаётся заранее отрендеренный HTML, для пользователей — обычный JS-сайт.

> Важно: Google явно не рекомендует такой формат. Более подробно в документации – [https://developers.google.com/search/docs/crawling-indexing/javascript/dynamic-rendering?hl=ru](https://developers.google.com/search/docs/crawling-indexing/javascript/dynamic-rendering?hl=ru)

**Преимущества**
- решает SEO-проблемы без полной переработки сайта
- можно внедрить поверх существующего CSR
- стабилизирует индексацию

**Недостатки**
- отдельная логика для роботов
- риск рассинхронизации контента
- усложнение поддержки
- потенциальные претензии со стороны поисковиков при некорректной реализации

##### Incremental Static Regeneration (ISR)

**В чём смысл**  
Комбинация SSG и SSR: страницы генерируются статически, но могут автоматически обновляться через заданные интервалы.

**Преимущества**
- высокая скорость
- актуальность контента
- хорошо масштабируется
- подходит для каталогов и карточек товаров

**Недостатки**
- сложнее в настройке
- зависит от конкретного фреймворка (Next.js и аналоги)
- обновления не всегда мгновенные

##### Гидратация (Hydration)

**В чём смысл**  
Гидратация — это процесс, при котором JavaScript «подключается» к уже готовому HTML, полученному с сервера (SSR / SSG / ISR), и делает страницу интерактивной. Контент уже присутствует в HTML, а JS лишь навешивает события и начинает управлять состоянием.

Проще:

> Сервер отдал HTML с контентом → JavaScript «оживил» страницу

**Преимущества**
- контент доступен сразу (SEO-friendly)
- быстрый первый рендер и LCP
- полноценная интерактивность страницы
- объединяет плюсы SSR и SPA

**Недостатки**
- JavaScript всё равно нужно скачать и выполнить
- возможны ошибки гидратации
- при сбоях возможна перерисовка DOM
- увеличение нагрузки на клиент, особенно на мобильных устройствах

**SEO-важные нюансы**
- при ошибках гидратации HTML может быть пересобран или очищен
- возможны расхождения между контентом для пользователя и поискового робота
- ошибки гидратации сложно диагностировать стандартными SEO-инструментами
- критично для карточек товаров, категорий и статей

##### Лучшее для SEO

Для SEO наилучшим вариантом являются стратегии, при которых **основной контент страницы присутствует в HTML сразу при загрузке**, без ожидания выполнения JavaScript: это **SSG и ISR**, а также **SSR** как надёжная альтернатива. Они обеспечивают стабильную индексацию, быстрый LCP и минимальную зависимость от корректного JS-рендеринга у поисковых систем. Чистый CSR остаётся самым рискованным вариантом и допустим только для незначимого контента, а prerendering (dynamic rendering) следует рассматривать как временный компромисс, а не долгосрочное SEO-решение.

### JS события

Пользовательские события (onclick, onhover, scroll и т.п.) являются проблемой, потому что поисковые роботы не выполняют сценарии.

Например 1. Тут контент `<div id="extraContent" class="hidden"> <p>Отзывы и советы по товару — изначально скрыты, но доступны в DOM.</p> </div>` уже есть в DOM, но скрыт стилями. Это хорошая реализация, т.к. роботы считают этот контент. 

```html
<h1>Карточка товара</h1>
  
  <style>
  /* Контент скрыт визуально, но присутствует в DOM */
  .hidden {
    display: none;
  }
</style>

<!-- Критический SEO-контент: всегда видим для робота -->
<p>Описание товара, характеристики, ссылки — доступны сразу.</p>

<!-- Дополнительный контент, который можно скрыть -->
<div id="extraContent" class="hidden">
  <p>Отзывы и советы по товару — изначально скрыты, но доступны в DOM.</p>
</div>

<!-- Кнопка для пользователя -->
<button onclick="toggleContent()">Показать отзывы</button>

<script>
  function toggleContent() {
    // Клик просто переключает видимость через CSS
    // Контент уже есть в DOM, поэтому робот его видит
    const content = document.getElementById('extraContent');
    content.classList.toggle('hidden');
  }
</script>
```

Например 2. Тут контент изначально отсутствует в DOM и загружается налету скриптом. Поэтому для поиска этот контент остается недоступным. В данном случае обратите внимание где находится текст. Он находится в области видимости JS, т.е. в html они никак недоступен. И он там не появится, пока не кликнуть на кнопку, поэтому и роботы его вряд ли увидят.

```html
<h1>Карточка товара</h1>

<style>
  /* Класс для скрытия */
  .hidden {
    display: none;
  }
</style>

<!-- Критический SEO-контент: всегда видим для робота -->
<p>Описание товара, характеристики, ссылки — доступны сразу.</p>

<!-- Кнопка для пользователя -->
<button id="showBtn">Показать отзывы</button>

<script>
  // Контент изначально нет в DOM
  const reviewsHTML = `
    <div id="extraContent">
      <p>Отзывы и советы по товару — подгружаются только после клика.</p>
    </div>
  `;

  // Обработчик клика: создаем и вставляем контент в DOM
  document.getElementById('showBtn').addEventListener('click', function() {
    if (!document.getElementById('extraContent')) {
      document.body.insertAdjacentHTML('beforeend', reviewsHTML);
    }
  });
</script>
```

Если контент появляется **только после действий пользователя** — например: клик, скролл, наведение, разрешение — **поисковый робот его не увидит**.

**Почему:**
- Роботы не имитируют пользовательские действия по умолчанию
- Контент, генерируемый «на лету» через JS после события, **не входит в DOM на момент рендеринга**, поэтому **не индексируется**

**Примеры ошибок:**
- раскрывающиеся блоки с текстом или характеристиками товара, которые создаются JS после клика
- скрытые фильтры, которые загружаются только при скролле
- кнопки «показать больше», создающие новые элементы DOM

**SEO-правила:**
- основной SEO-контент должен быть **доступен сразу в HTML или через гарантированный рендер JS**
- интерактивные элементы допустимы только для **дополнительного или второстепенного контента**, который не влияет на полноту страницы для поисковых систем

> Если критичный контент зависит от onclick / scroll / hover — для робота он **не существует**.
### Асинхронная загрузка

**В чём смысл**  
`async` и `defer` — это способы загрузки JavaScript, которые оптимизируют скорость страницы:
- **async** — скрипт загружается параллельно с HTML и выполняется **сразу после загрузки**, не дожидаясь остальных скриптов.
- **defer** — скрипт загружается параллельно, но выполняется **после построения всего DOM**, сохраняя порядок подключения.

**Преимущества**
- ускоряют загрузку страницы
- уменьшают блокировку HTML
- повышают показатели LCP и CLS

**Риски для SEO**
- **async** может запускать скрипт **в неожиданное время**, что приводит к неполному формированию DOM или «потерянному» контенту для робота
- **defer** безопаснее, но если критический контент формируется только через JS, роботу нужно дождаться выполнения скриптов
- ошибки или медленные скрипты могут **оставить SEO-контент недоступным**
- поиск и индексация зависят от **скорости загрузки и стабильности JS**

**SEO-правила**
- критический контент (H1, текст, ссылки, хлебные крошки) должен быть **доступен в HTML или через гарантированный рендер JS**
- async лучше использовать для **второстепенных скриптов**, например аналитики, виджетов, отзывов
- defer можно использовать для основных скриптов приложения, если рендеринг гарантирован и стабилен

> Основное правило: контент для робота **не должен зависеть от того, успел ли скрипт загрузиться и выполниться**.

## Как роботы LLM рендерят JS

**Почти никак**.

В отличие от Googlebot, который использует полноценный браузерный движок (Headless Chrome) для выполнения скриптов, роботы LLM (например, GPTBot от OpenAI или CCBot) обычно работают как текстовые парсеры.

Как это происходит на деле:

- **Текстовый захват:** Большинство LLM-краулеров скачивают только **исходный HTML-код** страницы. Если ваш контент подгружается через JavaScript (например, в React-приложении без SSR), робот увидит «пустую оболочку» или надпись «Please enable JavaScript».
- **Отсутствие рендеринга:** LLM не «ждут» выполнения скриптов, не кликают по кнопкам и не прокручивают страницу для ленивой загрузки (lazy load).
- **Использование посредников:** Некоторые современные системы (например, Perplexity или поисковые агенты) могут использовать сторонние API (вроде Selenium или специальных парсеров), которые сначала рендерят страницу в браузере и передают LLM уже готовый текст.
- **Индексация через поисковики:** Часто LLM получают информацию не напрямую с вашего сайта, а из индексов Google или Bing. Если Googlebot смог отрендерить ваш JS, то и LLM косвенно «увидит» этот контент через поисковую выдачу. 

**Итог:** Для LLM ваш сайт должен быть понятен на уровне чистого HTML. Если критически важный текст скрыт за JS-рендерингом, роботы ИИ его, скорее всего, проигнорируют.

Более подробно можно ознакомиться в статье – [https://www.clickrank.ai/llms-render-javascript/](https://www.clickrank.ai/llms-render-javascript/)
